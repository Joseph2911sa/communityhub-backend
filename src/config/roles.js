/**
 * Roles de usuario soportados por el sistema.
 * Ver sección 6 (Roles) del enunciado del proyecto.
 */
export const ROLES = Object.freeze({
  ADMIN: 'admin',
  ORGANIZER: 'organizer',
  USER: 'user',
});

export const ROLE_VALUES = Object.values(ROLES);
